FROM node:lts-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev && npm cache clean --force

COPY . .
EXPOSE 3000
CMD ["node", "src/index.js"]   # or your actual entrypoint
